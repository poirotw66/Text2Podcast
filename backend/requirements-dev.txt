# Test-only dependencies, layered on top of requirements.txt. Every version is
# pinned to a known-good release (matching the reproducibility policy in
# requirements.txt) -- re-verify with `pip index versions <pkg>` before bumping.
#
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Run with:     pytest   (from the backend/ directory; see pytest.ini)
pytest==8.4.2
pytest-timeout==2.4.0
